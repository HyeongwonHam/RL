````markdown
# 🤖 LiDAR-based Maze Exploration with PPO Variants

이 프로젝트는 **강화학습(Reinforcement Learning)**을 이용하여 에이전트가 2D LiDAR 센서 정보만으로 미지(Unknown)의 미로 환경을 효율적으로 탐색하고 지도를 작성(Mapping)하도록 훈련하는 연구입니다.

기본적인 **PPO(Proximal Policy Optimization)** 알고리즘과 부분 관측성(Partially Observable) 문제를 극복하기 위한 **기억 기반 변형 모델(History-PPO, Recurrent-PPO, CNN-PPO)**을 구현하고 성능을 비교 분석했습니다.

---

## 📌 주요 특징 (Key Features)

* **Lightweight Simulation**: Python과 NumPy 기반의 자체 경량 시뮬레이터를 구축하여 고속 학습 가능.
* **Procedural Maze Generation**: `Recursive Backtracker` 알고리즘과 개방도(Openness) 계수를 적용하여 매 에피소드마다 새로운 구조의 미로 생성.
* **Geometric Ray-casting**: 물리 엔진 없이 기하학적 연산만으로 정밀한 2D LiDAR 센서 모델링.
* **Multi-Model Comparison**: Baseline(PPO)부터 고급 모델(C-PPO, R-PPO)까지 4가지 알고리즘 구현 및 비교.

---

## 🛠️ 설치 및 요구사항 (Installation)

이 프로젝트는 Python 3.8+ 환경에서 실행하는 것을 권장합니다.

```bash
# 저장소 클론
git clone [https://github.com/YourUsername/Your-Repo-Name.git](https://github.com/YourUsername/Your-Repo-Name.git)
cd Your-Repo-Name

# 필수 라이브러리 설치
pip install numpy torch matplotlib pandas pygame
````

-----

## 🚀 실행 방법 (Usage)

### 1\. 알고리즘 훈련 (Training)

지정된 알고리즘으로 훈련을 시작합니다. (기본 2000 에피소드)

```bash
# 기본 PPO 훈련
python project.py --algo ppo --mode train

# 변형 모델 훈련 (rppo, hppo, cppo 중 선택)
python project.py --algo cppo --mode train
```

  * `--openness`: 미로의 개방도 설정 (기본 0.6, 0.0\~1.0)
  * `--render`: 학습 과정을 시각화하여 확인 (학습 속도가 느려질 수 있음)

### 2\. 순차적 전체 실험 (Run All Experiments)

모든 알고리즘(PPO -\> H-PPO -\> R-PPO -\> C-PPO)을 순차적으로 훈련합니다.

```bash
python project.py --algo all
```

### 3\. 테스트 및 시각화 (Testing)

훈련된 모델(`saved_models/`)을 불러와 성능을 테스트하고 주행 화면을 렌더링합니다.

```bash
python project.py --algo cppo --mode test --render
```

### 4\. 결과 그래프 그리기 (Plotting)

`logs/` 폴더에 저장된 학습 로그를 바탕으로 성능 비교 그래프를 생성합니다.

```bash
python plot_results.py
```

> 실행 후 `plots/` 폴더에 결과 이미지가 저장됩니다.

-----

## 🧠 알고리즘 및 모델 구조 (Algorithms)

| 알고리즘 | 설명 | 특징 |
| :--- | :--- | :--- |
| **PPO** | Standard MLP | 현재 시점의 센서 데이터(38차원)만 사용하는 Baseline 모델. |
| **H-PPO** | History Stacking | 과거 3개의 프레임을 연결(Concat)하여 입력. 단기 속도/가속도 정보 파악. |
| **R-PPO** | Recurrent (GRU) | GRU 셀을 도입하여 긴 시계열 데이터(Context)를 기억. |
| **C-PPO** | 1D CNN | 1D Convolution을 사용하여 시계열 데이터에서 시공간적 특징 추출. |

-----

## 🌍 환경 구성 (Environment Detail)

### State Space (38-dim)

  * **LiDAR**: 36개의 Ray-casting 거리 정보 (0.0 \~ 1.0 정규화)
  * **Previous Action**: 이전 스텝의 선속도, 각속도 (관성 반영)

### Action Space (Continuous)

  * **Linear Velocity**: 전진 속도 (-1.0 \~ 1.0)
  * **Angular Velocity**: 회전 속도 (-1.0 \~ 1.0)

### Reward System

  * **Coverage (+)**: 새롭게 밝혀진 그리드 셀 수에 비례.
  * **Frontier (+)**: 미탐사 구역에 가까워질수록 보상 부여.
  * **Penalty (-)**: 충돌, 제자리 회전, 시간 경과에 대한 페널티.

-----

## 📊 실험 결과 (Experimental Results)

총 2000 에피소드 학습 결과, \*\*메모리 기능이 있는 모델(C-PPO, R-PPO)\*\*이 우수한 성능을 보였습니다.

### 1\. 탐색 효율성 (Coverage)

  * **C-PPO**와 **R-PPO**는 약 250 에피소드 만에 80% 이상의 Coverage에 도달하며 가장 빠른 수렴 속도를 보였습니다.
  * **H-PPO**는 차원의 저주로 인해 초기 학습이 지연되는 현상을 보였습니다.

### 2\. 주행 안정성 (Return)

  * **C-PPO**가 가장 높은 평균 보상을 기록하며, 불필요한 회전이나 충돌 없는 효율적인 주행 정책을 학습했음을 입증했습니다.

-----

## ⚙️ 하이퍼파라미터 (Hyperparameters)

| Parameter | PPO / H-PPO / R-PPO | C-PPO |
| :--- | :---: | :---: |
| **Learning Rate** | 3e-4 | **1e-4** |
| **Batch Size** | 64 | **512** |
| **Update Interval** | 2048 | **4096** |
| **Gamma** | 0.99 | 0.99 |
| **Entropy Coef** | 0.01 | **0.025** |

-----

## 👥 팀원 (Team)

  * **남규범**: 3D PyBullet 환경 구축 및 비교 실험
  * **함형원**: 2D 시뮬레이션 환경 구축 및 알고리즘(PPO variants) 구현

-----

## 📝 License

This project is licensed under the MIT License.

```

### 추가 팁
* `plots/coverage_comparison.png` 와 같은 이미지 경로가 실제 파일 위치와 맞는지 확인 후 업로드하세요.
* `requirements.txt` 파일을 만들어 함께 올리면 사용자들이 더 편하게 설치할 수 있습니다. (`pip freeze > requirements.txt`)
```